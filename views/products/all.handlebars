<h1>Produtos</h1>

{{#each products}}

  <div class="product">
    <h3>{{this.name}}</h3>
    <p>R${{this.price}}</p>
    <div class="actions">
      <a href="/products/{{this._id}}">Ver</a>
    </div>
  </div>
{{/each}}